info:
  name: Get an app credential
  type: http

http:
  method: GET
  url: "{{base_url}}/app-credentials/:app_credential_id"
  params:
    - name: app_credential_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
