info:
  name: Create an app credential on an app
  type: http

http:
  method: POST
  url: "{{base_url}}/apps/:app_id/app-credentials"
  params:
    - name: app_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
