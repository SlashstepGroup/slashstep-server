info:
  name: Create an app on the server
  type: http

http:
  method: POST
  url: "{{base_url}}/apps"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
