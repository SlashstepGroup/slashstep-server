info:
  name: List delegation policies
  type: http

http:
  method: GET
  url: "{{base_url}}/delegation-policies"
  params:
    - name: query
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
