info:
  name: List actions
  type: http

http:
  method: get
  url: "{{base_url}}/actions"
  params:
    - name: query
      value: ""
      type: query
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
