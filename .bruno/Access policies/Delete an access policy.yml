info:
  name: Delete an access policy
  type: http

http:
  method: DELETE
  url: "{{base_url}}/:access_policy_id"
  params:
    - name: access_policy_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
