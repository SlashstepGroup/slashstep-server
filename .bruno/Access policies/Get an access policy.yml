info:
  name: Get an access policy
  type: http

http:
  method: GET
  url: "{{base_url}}/access-policies/:access_policy_id"
  params:
    - name: access_policy_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
