info:
  name: Create an access policy on an item connection type
  type: http
  seq: 1

http:
  method: POST
  url: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
