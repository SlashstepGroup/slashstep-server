info:
  name: List configurations
  type: http

http:
  method: get
  url: "{{base_url}}/configurations"
  params:
    - name: query
      value: ""
      type: query
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
