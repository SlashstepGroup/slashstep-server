info:
  name: Get a configuration
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/configurations/:configuration_id"
  params:
    - name: configuration_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
